<div class="container mt-4">
    <% if (successMessage) { %>
        <div class="alert alert-success">
            <%= successMessage %>
        </div>
    <% } %>
    <% if (errorMessage) { %>
        <div class="alert alert-danger">
            <%= errorMessage %>
        </div>
    <% } %>
    <h1>Painel de Administração</h1>
    <p>Bem-vindo ao painel administrativo, <%= user.nome %>.</p>
    <a href="/logout" class="btn btn-danger">Sair</a>
</div>
