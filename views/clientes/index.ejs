<h1>Lista de Clientes</h1>
<table class="table mt-3">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Gênero</th>
            <th>Endereço</th>
            <th>Telefone</th>
            <th>Email</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        <% clientes.forEach(cliente => { %>
            <tr>
                <td><%= cliente.nome %></td>
                <td><%= cliente.genero %></td>
                <td><%= cliente.endereco %></td>
                <td><%= cliente.fone %></td>
                <td><%= cliente.email %></td>
                <td>
                    <a href="/clientes/<%= cliente.cod %>" class="btn btn-info">Ver</a>
                    <a href="/clientes/<%= cliente.cod %>/edit" class="btn btn-warning">Editar</a>
                    <!-- Usando formulário com método DELETE para exclusão -->
                    <form action="/clientes/<%= cliente.cod %>" method="POST" style="display:inline;">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn btn-danger">Excluir</button>
                    </form>
                </td>
            </tr>
        <% }) %>
    </tbody>
</table>
