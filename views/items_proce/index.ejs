<h1>Listagem de Itens do Procedimento</h1>
<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Procedimento</th>
            <th>Produto</th>
            <th>Quantidade</th>
            <th>Ação</th>
        </tr>
    </thead>
    <tbody>
        <% items_proces.forEach(items_proce => { %>
            <tr>
                <td><%= items_proce.cod %></td> <!-- Use cod se for o campo correto -->
                <td><%= items_proce.procedimento %></td>
                <td><%= items_proce.produto %></td>
                <td><%= items_proce.quantidade %></td>
                <td>
                    <a href="/items_proces/<%= items_proce.cod %>" class="btn btn-primary" aria-label="Detalhes de <%= items_proce.produto %>">Detalhes</a>
                    <a href="/items_proces/<%= items_proce.cod %>/edit" class="btn btn-warning" aria-label="Editar <%= items_proce.produto %>">Editar</a>
                    <form action="/items_proces/<%= items_proce.cod %>?_method=DELETE" method="POST" style="display: inline;">
                        <button type="submit" class="btn btn-danger" aria-label="Excluir <%= items_proce.produto %>">Excluir</button>
                    </form>
                </td>
            </tr>
        <% }) %>
    </tbody>
</table>
<a href="/items_proces/new" class="btn btn-primary">Novo Item do Procedimento</a>
