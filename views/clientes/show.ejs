<h1 class="text-center my-4">Detalhes do Cliente</h1>

<div class="container p-4 shadow rounded bg-light">
    <p><strong>Nome:</strong> <%= cliente.nome %></p>
    <p><strong>Gênero:</strong> <%= cliente.genero || "Não informado" %></p>
    <p><strong>Endereço:</strong> <%= cliente.endereco %></p>
    <p><strong>Telefone:</strong> <%= cliente.fone %></p>
    <p><strong>Email:</strong> <%= cliente.email %></p>
    <p><strong>Data de Nascimento:</strong> <%= cliente.data_de_nascimento %></p>

    <div class="d-flex gap-2 mt-4">
        <a href="/clientes/<%= cliente.cod %>/edit" class="btn btn-warning">Editar</a>
        <form action="/clientes/<%= cliente.cod %>" method="POST" style="display:inline;">
            <input type="hidden" name="_method" value="DELETE">
            <button type="submit" class="btn btn-danger">Excluir</button>
        </form>
        <a href="/clientes" class="btn btn-secondary">Voltar</a>
    </div>
</div>
