<h1 class="text-center my-4">Detalhes do Procedimento</h1>

<div class="container p-4 shadow rounded bg-light">
    <p><strong>Código:</strong> <%= procedimento.cod %></p>
    <p><strong>Nome:</strong> <%= procedimento.nome %></p>
    <p><strong>Descrição:</strong> <%= procedimento.descricao %></p>
    <!-- Corrigido para garantir que o valor seja um número com duas casas decimais -->
    <p><strong>Valor:</strong> R$ <%= (parseFloat(procedimento.valor) || 0).toFixed(2) %></p>

    <div class="d-flex justify-content-between">
        <a href="/procedimentos" class="btn btn-primary">Voltar para a Lista</a>
    </div>
</div>
