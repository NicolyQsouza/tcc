<h1>Listagem de Agendas</h1>
<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Cliente</th>
            <th>Procedimento</th>
            <th>Profissional</th>
            <th>Forma de Pagamento</th>
            <th>Data</th>
            <th>Hora</th>
            <th>Ação</th>
        </tr>
    </thead>
    <tbody>
        <% agendas.forEach(agenda => { %>
            <tr>
                <td><%= agenda.cod %></td>
                <td><%= agenda.clienteNome %></td> <!-- Supondo que você tenha esse campo -->
                <td><%= agenda.procedimentoNome %></td> <!-- Supondo que você tenha esse campo -->
                <td><%= agenda.profissional %></td>
                <td><%= agenda.forma_pag %></td>
                <td><%= new Date(agenda.data).toLocaleDateString('pt-BR') %></td>
                <td><%= agenda.hora %></td>
                <td>
                    <a href="/agendas/<%= agenda.cod %>" class="btn btn-primary" aria-label="Ver detalhes da agenda">Detalhes</a>
                    <a href="/agendas/<%= agenda.cod %>/edit" class="btn btn-warning" aria-label="Editar agenda">Editar</a>
                    <form action="/agendas/<%= agenda.cod %>?_method=DELETE" method="POST" style="display: inline;" onsubmit="return confirm('Tem certeza que deseja excluir esta agenda?');">
                        <button type="submit" class="btn btn-danger" aria-label="Excluir agenda">Excluir</button>
                    </form>
                </td>
            </tr>
        <% }) %>
    </tbody>
</table>
<a href="/agendas/new" class="btn btn-primary">Nova Agenda</a>
